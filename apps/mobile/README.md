# `apps/mobile` — the sample app

The phone/tablet app that composes **every sample module** in the workspace. It is the reference
composition: each routing contract, DI group and fallback the shell supports has a real
contributor here. [`apps/admin`](../admin/README.md) composes the same shell with only `auth` and
`settings`.

## What it composes

From `app_manifest.yaml`:

| Module | Layers | What it shows |
|:--|:--|:--|
| `auth` | domain, data, feature | the full three-layer slice: login, token storage, session contracts |
| `home` | feature | BLoC, private Freezed events, a nav destination |
| `settings` | feature | a nav destination consuming another module's contract |
| `onboarding` | feature | `IAppEntryLocation`, the first-launch location |
| `splash` | feature | `IAppSplashScreen`, shown before the router exists |
| `dashboard` | feature | shell chrome only: bottom bar / navigation rail |
| `cache` | domain, data | a package-owned Drift database (no UI) |

The DI groups run in this order: `core` → the app's own registrations (`lib/firebase/`) →
`notifications` → `shell` → `ui` → `domain` → `data` → `feature` → `other`. The order matters. See
the comments in `app_manifest.yaml`.

## What is in here

```
apps/mobile/
├── app_manifest.yaml      which modules, which DI group order — edit this, then `composer sync`
├── pubspec.yaml           path dependencies generated between composer markers
├── env.dev  env.stg       --dart-define values, committed (no secrets)
├── lib/
│   ├── main.dart          runShellApp(configureDependencies: configureDependencies)
│   ├── di/injection.dart  generated by composer — never hand-edited
│   └── firebase/          firebase_module.dart + the gitignored firebase_options_<flavor>.dart
├── android/  ios/         native projects, three flavors each
└── fastlane/              release lanes (see docs/en/operations/02_fastlane_release.md)
```

To change what the app is made of, edit `app_manifest.yaml`, then from the repository root:

```bash
dart tools/composer/composer.dart sync --app mobile
flutter pub get
dart run build_runner build --workspace
```

## First run

From the repository root, set up the workspace once:

```bash
dart tools/workspace_setup/configure.dart
```

The app then needs files that are **gitignored** and absent from a fresh clone:

- `lib/firebase/firebase_options_dev.dart`, `_staging.dart` and `_prod.dart`. All three are
  imported unconditionally, so nothing compiles without them.
- `android/app/src/<flavor>/google-services.json` for each flavor you build on Android.

Create them with `dart tools/firebase/firebase_config.dart --app mobile`, which needs the Firebase
CLI installed and logged in. If you have no Firebase project, use the compile-only stubs instead.
With stubs the app builds, but push notifications and other Firebase features do not work. Both
paths are in [`01_setup.md` § 3](../../docs/en/getting-started/01_setup.md).

## Running and building

Always from **this directory**. The workspace root has no `android/` or `ios/` project.

```bash
cd apps/mobile
flutter run --flavor dev --dart-define-from-file=env.dev
flutter build apk --flavor dev --debug --dart-define-from-file=env.dev
```

| Flavor | Env file | Android application ID |
|:--|:--|:--|
| `dev` | `env.dev` (committed) | `com.example.codebase.dev` |
| `staging` | `env.stg` (committed) | `com.example.codebase.stg` |
| `prod` | `env.prod`: **create it yourself**, it is gitignored | `com.example.codebase` |

`env.prod` takes the same keys as `env.dev`: `BASE_URL`, `WEB_DOMAIN`, `APP_LINK_MODE`, `APP_NAME`.
VS Code users can pick **App (Dev)**, **App (Staging)** or **App (Prod)** from `.vscode/launch.json`.

## Signing

`android/key-dev.properties` and `android/keystore-dev.jks` are **committed on purpose**, so a
fresh clone builds and runs `dev` with no setup. Staging and prod read `key-stg.properties` and
`key.properties`. **If either file is absent, Gradle silently signs with the dev key.** Never ship
a production release with the dev keystore: it is public. Creating a release key, and the check to
run before a prod build, are in
[`02_fastlane_release.md` § 4](../../docs/en/operations/02_fastlane_release.md).

## More

- [`docs/en/README.md`](../../docs/en/README.md): the documentation hub
- [`docs/en/architecture/06_app_shell.md`](../../docs/en/architecture/06_app_shell.md): what the
  shell does between `main.dart` and the first frame
