name: feature_auth
description: "Authentication and credentials presentation feature package"
version: 1.0.0
publish_to: none

environment:
  sdk: ">=3.13.3 <4.0.0"
  flutter: ">=3.47.4"

resolution: workspace

dependencies:
  material_ui: ^1.1.1
  core_di:
    path: ../../../platform/foundation/contracts
  auth_api:
    path: ../api
  flutter:
    sdk: flutter
  domain_core:
    path: ../../../platform/layers/domain
  domain_auth:
    path: ../domain
  core_common:
    path: ../../../platform/foundation/common
  core_base_ui:
    path: ../../../platform/ui/design_system
  provider_state_management:
    path: ../../../platform/state/provider
  core_ui_kit:
    path: ../../../platform/ui/ui_kit
  injectable: ^3.0.0
  go_router: "^18.0.0"
  freezed_annotation: "^3.1.0"
  core_responsive:
    path: ../../../platform/ui/responsive
  provider: "^6.1.5+1"
  dynamic_logger: "^0.5.0"

  flutter_localizations:
    sdk: flutter
  intl: "^0.20.3"
dev_dependencies:
  build_runner: "^2.16.0"
  go_router_builder: "^4.4.0"
  injectable_generator: "^3.1.3"
  freezed: "^4.0.0-dev.3"
  flutter_test:
    sdk: flutter

flutter:
  generate: true
  uses-material-design: true
