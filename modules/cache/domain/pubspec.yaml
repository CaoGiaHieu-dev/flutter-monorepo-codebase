name: domain_cache
description: "Sample module: a Drift-backed key-value cache — domain layer"
version: 1.0.0
publish_to: none

environment:
  sdk: ">=3.13.3 <4.0.0"
  flutter: ">=3.47.4"

resolution: workspace

dependencies:
  domain_core:
    path: ../../../platform/layers/domain
  injectable: ^3.0.0
  freezed_annotation: "^3.1.0"

dev_dependencies:
  build_runner: "^2.16.0"
  injectable_generator: "^3.1.3"
  freezed: "^4.0.0-dev.3"
