name: feature_dashboard
description: "Dashboard and main screens presentation feature package"
version: 1.0.0
publish_to: none

environment:
  sdk: ">=3.13.3 <4.0.0"
  flutter: ">=3.47.4"

resolution: workspace

dependencies:
  material_ui: ^1.1.1
  core_di:
    path: ../../../platform/foundation/contracts
  core_responsive:
    path: ../../../platform/ui/responsive
  flutter:
    sdk: flutter
  get_it: ^9.2.1
  injectable: ^3.0.0
  go_router: "^18.0.0"

dev_dependencies:
  build_runner: "^2.16.0"
  injectable_generator: "^3.1.3"
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
