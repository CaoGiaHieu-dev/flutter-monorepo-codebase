/// Layout numbers of the splash screen, unscaled — `SplashPage` scales them
/// through `context` (RULE-30, RULE-33).
class SplashUiConstants {
  SplashUiConstants._();

  /// Side of the logo (scaled with `r`, so it stays square).
  static const double LOGO_SIZE = 64;
}
