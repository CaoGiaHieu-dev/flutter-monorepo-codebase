name: feature_splash
description: "Splash presentation feature package"
version: 1.0.0
publish_to: none

environment:
  sdk: ">=3.13.3 <4.0.0"
  flutter: ">=3.47.4"

resolution: workspace

dependencies:
  material_ui: ^1.1.1
  flutter:
    sdk: flutter
  core_base_ui:
    path: ../../../platform/base_ui
  core_di:
    path: ../../../platform/di
  get_it: ^9.2.1
  injectable: ^3.0.0
  core_responsive:
    path: ../../../platform/responsive
dev_dependencies:
  build_runner: "^2.16.0"
  injectable_generator: "^3.1.3"

flutter:
  generate: true
