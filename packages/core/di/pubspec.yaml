name: core_di
description: "Module core_di for codebase_template"
version: 1.0.0
publish_to: none

environment:
  sdk: ">=3.12.0 <4.0.0"
  flutter: ">=3.44.0"

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  domain_auth:
    path: ../../domain/auth
  go_router: "^17.3.0"
  get_it: ^9.2.1
  injectable: ^3.0.0
  freezed_annotation: "^3.1.0"
  json_annotation: "^4.12.0"

dev_dependencies:
  build_runner: "^2.15.1"
  injectable_generator: "^3.0.2"
  freezed: "^4.0.0-dev.3"
