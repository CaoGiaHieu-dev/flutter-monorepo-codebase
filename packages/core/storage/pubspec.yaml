name: core_storage
description: "Secure storage and caching helpers for codebase_template"
version: 1.0.0
publish_to: none

environment:
  sdk: ">=3.13.1 <4.0.0"
  flutter: ">=3.47.1"

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.5.5
  flutter_secure_storage: "10.3.1"
  encrypt: ^5.0.3
  get_it: ^9.2.1
  injectable: ^3.0.0
  dynamic_logger: "^0.5.0"
  core_common:
    path: ../common

dev_dependencies:
  build_runner: "^2.16.0"
  injectable_generator: "^3.1.3"
