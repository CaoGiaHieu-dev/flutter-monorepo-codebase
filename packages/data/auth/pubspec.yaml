name: data_auth
description: "Module data_auth for codebase_template"
version: 1.0.0
publish_to: none

environment:
  sdk: ">=3.13.1 <4.0.0"
  flutter: ">=3.47.2"

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  core_storage:
    path: ../../core/storage
  domain_core:
    path: ../../domain/core
  domain_auth:
    path: ../../domain/auth
  data_core:
    path: ../core
  firebase_auth: "^6.5.7"
  cloud_firestore: "^6.8.0"
  google_sign_in: "^7.2.0"
  flutter_facebook_auth: "^7.2.0"
  get_it: ^9.2.1
  injectable: ^3.0.0
  freezed_annotation: "^3.1.0"
  json_annotation: "^4.12.0"
  retrofit: "^4.10.0"
  dio: "^5.11.0"

dev_dependencies:
  build_runner: "^2.16.0"
  injectable_generator: "^3.1.3"
  freezed: "^4.0.0-dev.3"
  json_serializable: "^6.14.1"
  retrofit_generator: "^10.2.8"
  flutter_test:
    sdk: flutter
