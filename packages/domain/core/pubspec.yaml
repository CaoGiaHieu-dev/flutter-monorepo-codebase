name: domain_core
description: "Module domain_core for codebase_template"
version: 1.0.0
publish_to: none

environment:
  sdk: ">=3.13.0 <4.0.0"
  flutter: ">=3.47.0"

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  core_common:
    path: ../../core/common
  get_it: ^9.2.1
  injectable: ^3.0.0
  freezed_annotation: "^3.1.0"
  json_annotation: "^4.12.0"

dev_dependencies:
  build_runner: "^2.16.0"
  injectable_generator: "^3.1.3"
  freezed: "^4.0.0-dev.3"
  json_serializable: "^6.14.1"
