name: feature_auth
description: "Authentication and credentials presentation feature package"
version: 1.0.0
publish_to: none

environment:
  sdk: ">=3.13.1 <4.0.0"
  flutter: ">=3.47.1"

resolution: workspace

dependencies:
  core_di:
    path: ../../core/di
  flutter:
    sdk: flutter
  domain_core:
    path: ../../domain/core
  domain_auth:
    path: ../../domain/auth
  core_common:
    path: ../../core/common
  core_base_ui:
    path: ../../core/base_ui
  provider_state_management:
    path: ../../core/provider_state_management
  core_ui_kit:
    path: ../../core/ui_kit
  get_it: ^9.2.1
  injectable: ^3.0.0
  go_router: "^18.0.0"
  freezed_annotation: "^3.1.0"
  json_annotation: "^4.12.0"
  flutter_screenutil: "^5.9.3"
  provider: "^6.1.5+1"
  dynamic_logger: "^0.5.0"

dev_dependencies:
  build_runner: "^2.16.0"
  go_router_builder: "^4.4.0"
  injectable_generator: "^3.1.3"
  freezed: "^4.0.0-dev.3"
  flutter_test:
    sdk: flutter

flutter:
  generate: true
