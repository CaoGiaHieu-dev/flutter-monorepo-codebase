name: feature_home
description: "Module feature_home for codebase_template"
version: 1.0.0
publish_to: none

environment:
  sdk: ">=3.13.0 <4.0.0"
  flutter: ">=3.47.0"

resolution: workspace

dependencies:
  core_di:
    path: ../../core/di
  flutter:
    sdk: flutter
  core_common:
    path: ../../core/common
  domain_auth:
    path: ../../domain/auth
  core_base_ui:
    path: ../../core/base_ui
  provider_state_management:
    path: ../../core/provider_state_management
  bloc_state_management:
    path: ../../core/bloc_state_management
  flutter_bloc: "^9.1.1"

  get_it: ^9.2.1
  injectable: ^3.0.0
  go_router: "^17.5.0"
  freezed_annotation: "^3.1.0"
  json_annotation: "^4.12.0"
  flutter_screenutil: ^5.9.3

dev_dependencies:
  build_runner: "^2.16.0"
  go_router_builder: "^4.4.0"
  injectable_generator: "^3.1.3"
  freezed: "^4.0.0-dev.3"

flutter:
  generate: true
