name: feature_onboarding
description: "Onboarding presentation feature package"
version: 1.0.0
publish_to: none

environment:
  sdk: ">=3.13.1 <4.0.0"
  flutter: ">=3.47.2"

resolution: workspace

dependencies:
  core_di:
    path: ../../core/di
  flutter:
    sdk: flutter
  core_common:
    path: ../../core/common

  get_it: ^9.2.1
  injectable: ^3.0.0
  go_router: "^18.0.0"
  flutter_screenutil_plus: "^1.6.0"
dev_dependencies:
  build_runner: "^2.16.0"
  go_router_builder: "^4.4.0"
  injectable_generator: "^3.1.3"

flutter:
  generate: true
