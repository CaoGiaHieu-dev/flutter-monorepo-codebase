name: feature_settings
description: "Settings presentation feature package (sample)"
version: 1.0.0
publish_to: none

environment:
  sdk: ">=3.13.1 <4.0.0"
  flutter: ">=3.47.1"

resolution: workspace

dependencies:
  core_di:
    path: ../../core/di
  flutter:
    sdk: flutter
  core_common:
    path: ../../core/common
  core_base_ui:
    path: ../../core/base_ui
  get_it: ^9.2.1
  injectable: ^3.0.0
  go_router: "^18.0.0"
  flutter_screenutil: ^5.9.3

dev_dependencies:
  build_runner: "^2.16.0"
  go_router_builder: "^4.4.0"
  injectable_generator: "^3.1.3"

flutter:
  generate: true
