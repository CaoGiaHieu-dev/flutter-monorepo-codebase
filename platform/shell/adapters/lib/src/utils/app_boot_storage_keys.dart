/// Physical storage key owned exclusively by `platform_shell_adapters`' `AppBootStorage`.
class AppBootStorageKeys {
  AppBootStorageKeys._();

  static const String VIEWED_ONBOARD = 'viewed_onboard';
}
