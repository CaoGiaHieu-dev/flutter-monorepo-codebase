name: provider_state_management
description: "State management base structures and controllers for codebase_template"
version: 1.0.0
publish_to: none

environment:
  sdk: ">=3.13.3 <4.0.0"
  flutter: ">=3.47.4"

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  material_ui: ^1.1.1
  provider: "^6.1.5+1"
  injectable: ^3.0.0
  core_common:
    path: ../../foundation/common
  domain_core:
    path: ../../layers/domain
  core_responsive:
    path: ../../ui/responsive
  freezed_annotation: "^3.1.0"

dev_dependencies:
  build_runner: "^2.16.0"
  injectable_generator: "^3.1.3"
  freezed: "^4.0.0-dev.3"
  flutter_test:
    sdk: flutter
