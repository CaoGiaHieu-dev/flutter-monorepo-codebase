# Paths the documentation names that are CORRECTLY absent from a fresh clone.
#
# One repo-rooted path per line. A `#` starts a comment, and an entry without
# a reason is not allowed — the moment this file becomes a list of paths
# somebody silenced, the check stops being worth running. There are exactly
# three legitimate reasons, and a new entry must fit one of them:
#
#   (1) GENERATED  — build_runner or a tool writes it; it is gitignored.
#   (2) SECRET     — credentials, deliberately never committed.
#   (3) TUTORIAL   — a file the reader is told to CREATE, or a placeholder
#                    name standing in for "your module here".
#
# Anything else is drift: fix the document instead.

# --- (1) Generated ---------------------------------------------------------
# Written by `dart run build_runner build -d --workspace`. The docs cite it
# constantly because it is the only place an eager-singleton ordering bug
# (AGENTS.md §18) is visible.
apps/mobile/lib/di/injection.config.dart
# Flutter build output.
apps/mobile/build/app/outputs/flutter-apk/app-dev-debug.apk

# --- (2) Secrets -----------------------------------------------------------
# Production env values. `apps/mobile/env.dev` and `apps/mobile/env.stg` ARE committed;
# prod is not, which is the point the setup guide is making.
apps/mobile/env.prod
# Android release signing. `01_cicd.md` explains how CI materialises both.
apps/mobile/android/keystore.jks
apps/mobile/android/key.properties
# Per-flavor Firebase config written by tools/firebase/firebase_config.dart;
# gitignored because it carries the project's own Firebase identifiers.
apps/mobile/android/app/src/<flavor>/google-services.json
# A Gemini API key the code_review tool saves when asked to; gitignored.
tools/code_review/.gemini_api_key
# Fastlane credentials. The repo ships `apps/mobile/fastlane/Config.example.yaml`.
apps/mobile/fastlane/Config.yaml

# --- (3) Tutorial targets and placeholders ---------------------------------
# `11_design_system.md` walks the reader through creating this file; it does
# not exist until they do.
platform/base_ui/lib/src/styles/app_elevation.dart
# `01_new_feature.md` builds `profile` end to end as the worked example.
modules/profile/feature
modules/profile/feature/l10n.yaml
# `x` is the stand-in for the reader's module name in the barrel skill.
modules/x/domain/lib
modules/x/data/lib

# --- (3) Target layout, not current layout ---------------------------------
# `.agents/RESTRUCTURE.md` is the migration plan: these are the directories a
# later step creates. They are removed from this list as each step lands, which
# is what stops the plan from quietly describing a tree nobody built —
# `apps/mobile` came off this list the moment step 6b created it, and
# `apps/admin` when step 7c did. `modules/catalog` came off when step 2's
# target was revised and no longer planned it. The section is empty now.

# --- Patterns (spans containing <placeholder>, * or {a,b}) -----------------
# Checked since placeholders stopped being skipped: at least one real path
# must fit. These are the ones that correctly match nothing.

# (1) Generated: flutter gen-l10n output, gitignored.
modules/<f>/feature/lib/src/gen/language
# (1) Generated: release build output, named per flavor.
apps/mobile/build/app/outputs/flutter-apk/app-<flavor>-release.apk
# (3) Tutorial: CLAUDE.md tells the reader to create their own table here.
modules/<module>/<layer>/lib/src/database/tables
# History: RESTRUCTURE.md's own record of the layout the relayout replaced.
# The plan has to be able to name what it moved away from.
packages/core/*
packages/{domain,data,features}/<x>
packages/**/l10n.yaml
packages/*/*
