import 'package:domain_core/domain_core.dart';
import 'package:injectable/injectable.dart';
import 'package:provider_state_management/provider_state_management.dart';

@injectable
class {{pascalNameInput}}Provider extends BaseProvider<Object> {
  {{pascalNameInput}}Provider();

  /// Called once, right after construction — `BaseProvider` schedules it.
  /// A method with any other name would never run, and the page would show
  /// its loading state forever.
  @override
  Future<void> initialize() async {
    await super.initialize();
    await executeOperation(
      OperationConfig(
        operation: () async {
          return const Result.success(Object());
        },
      ),
    );
  }
}
