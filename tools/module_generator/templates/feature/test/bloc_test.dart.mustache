import 'package:bloc_state_management/bloc_state_management.dart';
import 'package:{{moduleName}}/{{moduleName}}.dart';
import 'package:flutter_test/flutter_test.dart';

/// [{{pascalNameInput}}Bloc] on its own, no widget tree.
///
/// Generated by tools/module_generator. When the bloc takes use cases, build
/// it here with hand-written fakes of their repositories (see
/// modules/home/feature/test/home_profile_bloc_test.dart) rather than from DI.
void main() {
  test('starts in the initial state', () {
    final bloc = {{pascalNameInput}}Bloc();
    addTearDown(bloc.close);

    expect(bloc.state, const BlocViewState<{{pascalNameInput}}StateData>.initial());
  });

  test('started emits loading, then success', () async {
    final bloc = {{pascalNameInput}}Bloc();
    addTearDown(bloc.close);

    bloc.add(const {{pascalNameInput}}Event.started());

    await expectLater(
      bloc.stream,
      emitsInOrder([
        const BlocViewState<{{pascalNameInput}}StateData>.loading(),
        const BlocViewState<{{pascalNameInput}}StateData>.success(
          {{pascalNameInput}}StateData(),
        ),
      ]),
    );
  });
}
