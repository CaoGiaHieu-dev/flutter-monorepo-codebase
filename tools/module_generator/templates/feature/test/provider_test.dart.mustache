import 'package:{{moduleName}}/{{moduleName}}.dart';
import 'package:flutter_test/flutter_test.dart';

/// [{{pascalNameInput}}Provider] on its own, no widget tree.
///
/// Generated by tools/module_generator. When the provider takes use cases,
/// build it here with hand-written fakes of their repositories (see
/// modules/auth/feature/test/auth_provider_test.dart) rather than from DI.
void main() {
  TestWidgetsFlutterBinding.ensureInitialized();

  test('initialize() runs on construction and ends in success', () async {
    final provider = {{pascalNameInput}}Provider();
    addTearDown(provider.dispose);

    // BaseProvider schedules initialize() itself; this waits for it.
    await provider.ensureInitialized();

    expect(provider.isSuccess, isTrue);
    expect(provider.isError, isFalse);
  });
}
