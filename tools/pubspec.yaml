name: core_tools
description: "Monorepo CLI tools"
version: 1.0.0
publish_to: none

environment:
  sdk: ">=3.13.1 <4.0.0"
  flutter: ">=3.47.1"

resolution: workspace

dependencies:
  path: ^1.9.1
  glob: ^2.1.3
  yaml: ^3.1.3
  args: ^2.7.0
  http: ^1.6.0
  injectable: ^3.0.0
  mustache_template: ^2.0.5
