name: core_tools
description: "Monorepo CLI tools"
version: 1.0.0
publish_to: none

environment:
  sdk: ">=3.13.3 <4.0.0"
  flutter: ">=3.47.4"

resolution: workspace

dependencies:
  path: ^1.9.1
  glob: ^2.1.3
  yaml: ^3.1.3
  args: ^2.7.0
  http: ^1.6.0
  mustache_template: ^2.0.5

dev_dependencies:
  test: "^1.31.1"
